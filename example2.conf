*> Конфигурация базы данных
db_name: "production_db"
connection: struct {
    host = "db.example.com",
    port = 5432,
    user = "admin",
    password = "secret123",
    pool_size = 20
}
backup: struct {
    enabled = true,
    interval = 3600,
    retention_days = 7
}